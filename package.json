{
  "name": "codeviz",
  "private": true,
  "scripts": {
    "dev": "cd client && [ -d node_modules ] || npm install --legacy-peer-deps && npx vite",
    "build": "cd client && npm install --legacy-peer-deps && npx vite build",
    "build:dev": "cd client && npm install --legacy-peer-deps && npx vite build --mode development",
    "preview": "cd client && npx vite preview"
  }
}
